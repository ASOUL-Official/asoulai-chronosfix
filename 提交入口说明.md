# AsoulAI ChronosFix（A-CFX）复赛提交入口说明

## 作品名称

AsoulAI ChronosFix：带证明的软件变更基础设施

## 队伍名称

AsoulAI

## 参赛方向

新智基座 Agent Infra · 方向三：软件研发全流程协同

## 代码仓库

https://github.com/ASOUL-Official/asoulai-chronosfix

## 在线 Demo

https://asoul-official.github.io/asoulai-chronosfix/

## 复赛提交材料

1. 更新版项目方案：
   - `submission/ChronosFix_复赛方案.pptx`
   - `submission/ChronosFix_复赛方案.pdf`
2. 可执行 AgentTeams 代码包：
   - `agentteams/chronosfix-team.yaml`
   - `agentteams/run_chronosfix_team.py`
   - `src/chronosfix/`
   - `demo.py`
3. 可运行 Demo：
   - 在线 Demo：GitHub Pages Repair Cockpit
   - 本地 Demo：`repair-cockpit/index.html`
4. 工程验证证据：
   - `evidence/proof-report.md`
   - `evidence/proof-bundle.json`
   - `evidence/trace.jsonl`
   - `evidence/run-log.jsonl`
   - `evidence/engineering-metrics.json`
   - `evidence/evaluation-report.md`
   - `evidence/agentteams-run.json`
   - `evidence/github-issue.md`
   - `evidence/github-pr.md`
   - `evidence/github-pr-diff.patch`
   - `evidence/github-pr-checks.json`
   - `evidence/github-review-audit.jsonl`

## 推荐评审阅读顺序

1. 先看 `submission/work-intro-500.txt`，快速了解作品定位。
2. 再看 `submission/ChronosFix_复赛方案.pptx` 或 PDF，了解场景闭环、创新点、官方 Infra 映射和商业价值。
3. 打开在线 Demo，按“事故事实→因果证明→故障族验证→三态门禁→证据护照→评测与沉淀”六步演示。
4. 查看 `docs/proof-carrying-change.md`，确认底层逻辑不是自动修 Bug，而是带证明的软件变更链。
5. 查看 `docs/semifinal-guide-matrix.md`，确认复赛要求逐项覆盖。
6. 查看 `docs/official-infra-mapping.md`，确认 AgentTeams、云 Skills、Nacos、Higress、PolarDB、UnifiedModel、RocketMQ、可观测链路的接口关系、必要性和替换成本。
7. 查看 `docs/interface-schema.md` 与 `docs/deployment-and-verification.md`，确认接口 Schema、部署配置、失败处理、权限、审批、回滚和审计机制。
8. 查看 `docs/github-issue-pr-flow.md`，确认事故如何进入 GitHub Issue / PR 协作流。
9. 查看 `evidence/` 目录，确认样例输入输出、日志、Trace、Metrics、评测报告、GitHub PR 草案和运行证据。

## 一句话介绍

AsoulAI ChronosFix（A-CFX）不是让 AI 自动修 Bug，而是把线上事故转成带证明的软件变更链：事故证据进入，反事实实验证明根因，缺陷基因验证补丁，RiskGate 完成审批，GitHub PR 携带证据护照，最后沉淀为可复用 Skill 和故障资产。它让研发质量从一次性救火变成可持续积累、可审计、可商业化的组织资产。

## 官方基座对齐

- 必选 AgentTeams：以 AgentTeams（原 Hiclaw）作为多 Agent 协同设计基点，说明角色编排、任务拆解、上下文传递、协同执行与状态追踪。
- 云 Skills：把本地 Skill 规格映射到阿里云 Skills 门户，说明鉴权、编排和端到端体验。
- Nacos：用于 Agent、Skill、Prompt、配置和运行时治理。
- Higress：作为模型、Agent 服务和外部工具调用统一入口，负责鉴权、路由、限流和观测。
- PolarDB for PostgreSQL：作为向量、长记忆、RAG、Trace 与审计日志的生产级候选数据层。
- UnifiedModel：统一 Incident、AgentRun、SkillCall、EvidencePassport、PatchCandidate 等实体。
- RocketMQ：用于事件驱动、异步任务调度、Agent 间消息协同和可靠通知。
- LoongSuite / AgentScope Studio / AgentLoop：用于推理轨迹、质量、性能、成本和可靠性评估。

## 核心创新

- 故障时间机器：用平行版本反事实实验证明根因。
- 缺陷基因实验室：从一次事故繁殖同源变体，防止补丁只修单一样例。
- 补丁竞赛：多个候选补丁按正确性、风险、成本和回滚策略竞争。
- 证据护照：补丁携带需求、因果、验证、风险、回滚和缺口声明。
- GitHub Issue / PR 模拟链路：事故进入 Issue，补丁进入 PR，检查与审计随证据链沉淀。
- Skill 自进化工坊：把事故处理经验沉淀为可复用 Skill 候选。
- 研发质量资产平台：把事故复盘转化为故障基因包、审计模板和 Skill 市场资产。

## 复现命令

```powershell
cd chronosfix
python -m unittest discover -s tests -p "test_*.py" -q
python demo.py --approve --output evidence
python agentteams/run_chronosfix_team.py --approve --output output/agentteams-latest
python demo.py --output output/no-approval
python evaluate.py --scenarios scenarios --output evidence/evaluation-corpus
python agentteams/runtime/validate_resources.py agentteams/runtime/chronosfix-resources.yaml
```

## 当前验证结论

- 36 项自动化测试与严格 JSON / JSONL 校验已通过。
- Demo 可生成 proof report、proof bundle、Trace、run-log、metrics、PR checks 与 run manifest。
- 11 份 AgentTeams v1beta1 资源声明已离线校验；本地入口生成 `compatible` 协作转录，不冒充真实 Worker 执行。
- 12 场景评测当前为 9/9 受支持诊断、1/1 冲突拒答、整体 10/12；两个未建模 Badcase 公开保留。
- 官方 SLS Skill 已完成只读适配器契约与 dry-run；真实查询仍需本地只读 RAM Profile。
- 无审批分支会进入 `blocked-awaiting-human`，证明风险门禁生效。
- Demo 前端标题已保持单行且不与窗口重叠。
